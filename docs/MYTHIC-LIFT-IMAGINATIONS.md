# the mythic lift ... imaginations log

*the living ideation log for the perfection-loop pass. per the loop doc: envision,
ideate, build in reps, and LOG the accessory ideas that surface during the build for
the next set. this file is the record. (docs/ is excluded from typecheck+lint.)*

## iteration 1 scorecard (honest, weighted, cited)

weighted overall **8.03 / 10** (target 9.0). no dimension below 6, so no cap.

| dimension (weight) | score | the single highest-leverage gap |
|---|---|---|
| wedge differentiation (x1.5) | 9.0 | provenance chain only half on-screen (one hook line, signal-cards only) |
| visual polish (x1.5) | 8.5 | one elevation note ... flat field of equal-weight cards, no focal hierarchy |
| feature completeness (x1.5) | 8.0 | campaigns can't send end-to-end (pg-boss executor deferred, no worker in repo) |
| code health (x1.0) | 8.0 | test suite lopsided; scrubVoice/flame/trigger/graph untested; 3 raw hex in unsub html |
| futuristic / wow (x1.0) | 7.0 | cmd+K only navigates; copilot reads like a terminal not a control surface |
| ux coherence (x1.0) | 7.0 | draft studio dead-ends at "copy to clipboard" ... the fork at the center of the motion |

## the build backlog (deduped from 18 imagine specs, all no-migration, wow-ranked)

- **REP A ... close the draft->send fork** (coherence, wow 9). wire enroll + a test-safe
  send into the DraftStudio winner angle, gated by the presend verdict already rendered.
- **REP B ... the copilot gets a body** (wow 9). GenChat renders tool parts as
  lifecycle-aware cards (leads landing, verify bar, 5 angle cards winner-ringed, send
  test/live pill), plan checklist pinned as a sticky run-rail. route already emits the parts.
- **REP C ... cmd+K becomes the copilot's keyboard twin** (wow 8). "ask gen '<q>'" row ->
  /gen?q=, live gc_contacts fuzzy-search rows -> contact drawer. registry kind + one read action.
- **REP D ... the beautify cluster** (visual 8.5):
  - D1 `.surface-focal` tier + one focal element per screen + Cinzel page titles + fix
    no-op hovers + snap type-scale back onto the ladder.
  - D2 HeroStat: count-up on load + tween re-climb on every win.
  - D3 realtime entrance choreography (reveal-up + reserved pulse) + faint per-tab aura.
- **REP E ... the provenance chain, fully on screen** (wedge 9->10). "why they're here"
  section in ContactDrawerPanel + richer ContactCard breadcrumb + unibox ContactRail.
  data already stamped on the row.
- **REP F ... campaigns that actually send** (feature 8). advanceDueEnrollments runner over
  compileRun + guardedSend (test-mode-safe), behind a secret-guarded route. the autonomous
  cron is a go/no-go for dom (world-movement gate), the runner + manual tick ship now.
- **REP G ... make "tested with fixtures" true** (code health 8). extend test/pure.ts:
  scrubVoice, flameScore, evaluateTrigger, validateGraph. fix the 3 raw hex in the unsub html.

## accessory ideas captured during build
*(appended as they surface ... the loop doc's "log for the next stage")*
- (none yet)
