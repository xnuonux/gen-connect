# pnpm 10 runs a full `pnpm install` deps check before every `pnpm <script>`.
# that check hard-fails on any un-acknowledged build script, so it blocked the
# whole ship chain. scripts run against the installed tree ... skip the gate.
verify-deps-before-run=false
